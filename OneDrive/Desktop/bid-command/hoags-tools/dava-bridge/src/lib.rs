//! dava_bridge — DAVA memory + Ollama/Claude interface.
//! Shared intelligence layer for all Hoags tools.
//! DAVA learns from every tool and gets smarter across all of them.

pub use hoags_core::memory::FieldMemory;
pub use hoags_core::context::{load_context_file, resolve_key};
